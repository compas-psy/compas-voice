REPLACE_WITH_NEW_KEY_SHA256
# Ожидаемый отпечаток ключа подписи (SHA-256 сертификата, строчными, без двоеточий).
# Заполняется учредителем при смене ключа — вместо строки выше, ИЛИ переменной
# репозитория EXPECTED_SIGNER_SHA256 (она имеет приоритет).
# Значение не секретно: это публичный отпечаток, его видно в любом APK.
# Пока стоит REPLACE_... — CI не подпишет и не выпустит сборку.
